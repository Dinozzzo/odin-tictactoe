function tictactoe() {
  const gameboard = {
    table: [1, 2, 3, 4, 5, 6, 7, 8, 9],
  };

  function player(name, symbol, score) {
    return {
      name,
      symbol,
      score,
    };
  }

  const gameController = {};
}
